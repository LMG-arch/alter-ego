# 计划：把主体补齐（对话 / 设置 / 插件加载 / Web UI）

> 目标日期 2026-09-16。依据 `docs/DESIGN.md` § 17 分册索引。
> 本文件是**施工计划**，不是设计文档——设计以 `docs/design/*.md` 为准，本文件只记录
> 「先做哪一块、为什么是这个顺序、哪一块这次不做」。

---

## 0. 进展（每批交付后回写这一节）

| 批次 | 状态 | 交付 |
| --- | --- | --- |
| **A · 对话主体** | ✅ 完成 | `sim/{budget,intents,stages/*,engine,conversation}.py`、`TickContext.llm()`、四个新 `Repository` Protocol + SQLite 实现、`alterego chat`（`cli_chat.py`） |
| **B · 插件加载** | ✅ 完成 | `alterego plugins {list,doctor,info,reload,reset}`（`cli_plugins.py`）。**刻意没做 `enable` / `disable`**：它要改的不是「一个值」，而是 `plugins.enabled` 列表里的**一个元素**，而列表目前只能整体渲染——命令行整体重写那一行会顺手吃掉用户写在对齐注释里的取舍记录。`list` / `info` 因此改成直接给出**能抄进配置文件的那一行**，把「增删一个元素」留给批次 C 的列表编辑控件 |
| **B · 设置** | ✅ 完成 | `kernel/settings.py` + `settings_catalog*.py`（95 条 / 17 段）+ `settings_write.py` + `[settings]` 段 + `alterego config {show,explain,get,set,schema}`（`cli_config.py`） |
| **C · Web UI** | ✅ 完成 | `channels/web/{app,deps,auth,sse,views,plugin}.py` + `routes/`（8 个模块 / 20 条路由）+ `static/`（单页 10 个标签）+ `alterego serve`（`cli_serve.py`）+ `[web]` 段（`kernel/config_web.py`）。测试：`test_web_routes.py`（62）、`test_web_app_middleware.py`（24）、`test_cli_serve.py`（19）、`test_web_{sse,auth,plugin,views}.py`（72）。**没做**：`/api/feed/{id}/comment`（回 503）、`/api/emotion` 的曲线、`/api/tick/{id}`、`/api/logs*`、相册、关系网/相册/日志三个标签——卡在仓储接口上，见 § 4 批次 D |
| **D · 接口一致性审计 + 插件开发指南** | ✅ 完成 | `docs/design/13-interface-consistency.md`（**23 条**差异 / 可复现的四步审计法）+ `docs/guide/plugin-development.md`（**写插件的唯一依据**，15 节）+ `tests/test_interfaces_consistency.py`（26）。代码侧修 **8** 条、文档侧改 **12** 条；另新建 `docs/guide/README.md`。⚠️ 最终交付物叫 `docs/guide/plugin-development.md`——**`docs/PLUGIN_GUIDE.md` 这个名字从未存在过**，当初的草稿里写错了它，`kernel/manifest.py` ×2 与 `interfaces/__init__.py` ×1 也跟着指错了地方 |

`AGENTS.md` § 3 与 § 7、`docs/design/10-settings-center.md` 开头的「尚未实现」两处横幅
已在批次 B 里回改——**机制落地而不回改横幅，文档就开始说谎**。

批次 C 交付时补了三件事，值得记在这里，因为它们都属于「没人写测试就永远发现不了」：

1. **`create_app` 从来没被任何测试调用过**。补上第一个集成测试之后，五分钟里就暴露出
   三个致命缺陷：路由装饰期 `assert`（`Annotated[..., Query(default=)]`）让整个应用
   **import 失败**；`/api/settings` 拿 `dict` 去调只接受 `Config` 的 `_flatten`，直接
   `AttributeError`；`/api/feed` 的 `total` 其实是「这次取了几条」。3083 个测试全绿，
   而 WebUI 一行都跑不起来。
2. **`kernel/config.py` 涨到 914 行**，超过 900 的硬上限。没有改上限，而是把 `[web]`
   段搬进 `kernel/config_web.py`（先例 `config_study.py`、`config_values.py`），
   `config.py` 回到 871 行并继续转出 `WebConfig`。
3. **测试之间共享 `logging` 全局状态**。`alterego serve` 会调 `setup_logging`，
   而它把 `alterego` 的 `propagate` 关掉；`caplog` 挂在 root 上，于是
   `test_cli_serve.py` 一旦排在 `test_holidays.py` 前面，后者就红。
   收在 `tests/conftest.py` 的 autouse 夹具里，而不是给某个文件打补丁。

---

## 1. 这次要解决的四个缺口

用户的原话是「主体的对话，设置，插件加载，webui 页面等这些主体部分先完成」。
对着代码盘一遍，四个块的现状是：

| 主体块 | 文档规定的位置 | 代码现状（本次实测） |
| --- | --- | --- |
| **对话主体** | `sim/engine.py`、`sim/stages/`（6 个）、`sim/intents/`、`sim/budget.py` | ❌ 全缺。`sim/` 只有 `consolidation/context/dataset/study/vault` |
| **设置** | `kernel/settings.py`（`Setting`/`SettingKind`/`SETTING_METADATA`）+ `alterego config` | ❌ 全缺。`[settings]` 段也不在 `templates/alterego.toml` |
| **插件加载** | `alterego plugins {list,doctor,enable,disable,reload,reset}` | ⚠️ **能力全在**（`PluginManager` 有 `health_all`/`reload`/`reset`/`enabled_ids`），**入口没有** |
| **Web UI** | `channels/web/{app,routes,sse,auth}.py` + `static/` + `alterego serve` | ❌ `channels/web/__init__.py` 是 89 行纯 docstring、零代码；`static/` 只有 `.gitkeep` |

> ⬆️ 上面这张表是**开工前**的实测，留着是为了对照「计划说的缺口」与「实际交付」。
> 现在的进展见 § 0。

另外两件事不在「主体」里，但卡在上面四块的路上：

- `TickContext.llm()` **不存在**（`04-simulation-loop.md` § 2.2 第 159 行规定了它）。没有它，
  阶段没法调模型，只能自己去 `ctx.registry` 里翻网关——那正是文档要避免的写法。
- `interfaces/repository.py` **没有会话写入口**。`list_conversation_messages()` 挂在
  `DatasetSourceRepository` 上，是**只读的、给数据集导出用的**。表（`conversation` /
  `message` / `budget_usage` / `activity_log` / `tick_log`）在 `001_initial.sql` 里**全都存在**，
  缺的只是协议与实现。

---

## 2. 顺序：为什么是「预算 → 意图 → 阶段 → 引擎 → 会话 → CLI/Web」

依赖是单向的，倒过来写任何一步都会写出没有调用方的死代码：

```
budget.py      纯函数，无依赖           ← 先写，因为它是 P3「机制约束」的落地点
intents/       纯数据，依赖 interfaces.IntentType
stages/        依赖 budget + intents + TickContext.llm()
engine.py      依赖 stages + repository
conversation.py 依赖 domain.conversation（已实现）+ gateway（已实现）+ repository
cli_chat.py    依赖 conversation + 组装根
channels/web/  依赖 conversation（对话页）+ settings（设置页）
```

**先做对话主体，因为它是最上面那块，也是最缺的。** 设置与插件加载都是「把已有的能力
露出一个入口」，一天之内能做完；Web UI 则要等对话路径存在才有页面可写。

---

## 3. 四条硬性约束（这次每一块都要过）

从 `docs/DESIGN.md` § 3 与 `scripts/check_architecture.sh` 的 23 项里挑出与本计划相关的：

| 约束 | 落在哪 |
| --- | --- |
| `sim/` 不得用 `datetime.now()` / `time.time()`（红线 15） | 引擎与阶段一律走 `ctx.virtual_now` |
| `sim/` 不得用全局 `random`（红线 14） | 一律走 `ctx.rng` |
| `sim/` 不得直连 LLM 端点（红线 20） | 只通过 `ctx.llm()` → `ServiceRegistry` 取网关 |
| `sim/` 不得 import `storage.sqlite`（红线 10） | 只认 `interfaces.repository` 的 Protocol |
| `kernel/` 不得出现 `fastapi`（红线 1） | Web 实现只能在 `channels/web/` |
| 单文件 ≤ 900 行（红线 23） | `kernel/config.py` **已经是 900/900**，设置中心的任何新配置段必须开卫星模块 |

---

## 4. 分批交付

每一批都按 `AGENTS.md` § 4 的六步走：写代码 → 写测试 → 跑全部门禁 → 同步文档 →
更新 CHANGELOG → 提交。

### 批次 A · 对话主体

| 文件 | 内容 |
| --- | --- |
| `sim/budget.py` | `BudgetUsage` / `BudgetDecision` / `check_budget()` / `record_sent()` / `record_no_reply()`，**纯函数**。五条有序检查按 `04-simulation-loop.md` § 5 的 940–1012 行 |
| `sim/intents.py` | 11 种内置意图的 `IntentType` 目录 + `select_candidates()` |
| `sim/stages/{sense,reflect,intention,act,express,persist}.py` | 六个阶段，`order` 10/30/50/70/90/110 |
| `sim/engine.py` | `SimulationEngine.tick()`：`on_tick_pre` → 六阶段 → `on_tick_post` → 合并 → 落库 → `tick.completed` |
| `sim/conversation.py` | `ConversationService.reply()`：入站消息 → 落库 → 组提示词 → 调模型 → 落库 → `OutboundMessage` |
| `sim/context.py` | 补 `TickContext.llm()`（走 `ServiceRegistry` 取 `LLMGateway`） |
| `interfaces/repository.py` | 新增 `ConversationRepository` / `BudgetRepository` / `ActivityLogRepository` / `TickLogRepository` 四个 Protocol |
| `storage/sqlite/repositories.py` | 上述四个的 SQLite 实现 |
| `cli_chat.py` | `alterego chat`（`--once` 单句、`--show-prompt` 看组出来的提示词） |
| `domain/__init__.py` | 若新增领域模块要登记 |

**为什么会话不走 tick 而要单独一条路径。** `04-simulation-loop.md` § 3.3 说 `reply` 意图
在有未读消息时权重 1.0，那是**主动行为的调度**；而用户在 Web 上打完字等回复，等不了一个
tick（`realtime` 模式 5 虚拟分钟 ≈ 5 分钟）。两条路径共用同一套纯函数
（`domain.conversation.decide_reply` 决定即时/正常/延迟），但触发时机不同：
**tick 决定「它想不想主动找你」，会话路径决定「你说了话它怎么回」。**

### 批次 B · 插件加载 + 设置

| 文件 | 内容 |
| --- | --- |
| `cli_plugins.py` | `alterego plugins {list,doctor,info,enable,disable,reload,reset}`，全部复用 `PluginManager` 已有方法 |
| `kernel/settings.py` | `Choice` / `Setting` / `SettingKind` / `CONFIG_DATACLASSES` / `SETTING_METADATA` / `get_setting_metadata()` / `all_settings()` / `infer_setting()`（第三层兜底） |
| `kernel/config_settings.py` | `[settings]` 段（4 个键）——**卫星模块**，因为 `config.py` 已经 900/900 |
| `cli_config.py` | `alterego config {show,explain,get,set,schema}` |
| `tests/test_settings_metadata.py` | 三条 CI 断言（`10-settings-center.md` § 4） |

`AGENTS.md` § 3 与 `docs/design/10-settings-center.md` 开头都写着「尚未实现」——
机制落地后**必须回改这两处**，否则文档就在说谎。

### 批次 C · Web UI

| 文件 | 内容 |
| --- | --- |
| `channels/web/plugin.py` | `WebChannel`：实现 `Channel` Protocol，`direction={"out"}`+`{"in"}`，注册进 `ServiceRegistry` |
| `channels/web/sse.py` | `sse_pack()` + 订阅者集合 + `broadcast()` |
| `channels/web/auth.py` | token/密码、`hmac.compare_digest`、`.alterego-token` |
| `channels/web/app.py` | FastAPI 应用装配（**import 在函数内**，缺失依赖时给可执行提示） |
| `channels/web/routes/{chat,status,feed,settings,plugins,stats}.py` | 路由 |
| `channels/web/static/{index.html,app.js,style.css}` | 单页、13 tab、无构建、SSE 优先 + 轮询回退 |
| `cli_serve.py` | `alterego serve [--host --port --no-web]` |

FastAPI 已经是 `pyproject.toml` 的 `web` optional extra（实测第 50–52 行），
与 `05-channels.md` § 3.1 一致——**不需要新 ADR**，只需要在文档里说清「Web 是可选层」。
`web/` 的实现**不能**被 `kernel/` import（红线 1），所以 `channels/web/app.py` 里
只在函数体内 `import fastapi`。

### 批次 D · 接口一致性审计 + 插件开发文档

交付物两份：

1. `docs/design/13-interface-consistency.md` —— 「文档承诺 vs 代码实际」对照表，
   以及为消除差异所做的修改。**不是改文案，是改机制**：差异能被测试或脚本钉住的，
   就加断言。
2. `docs/guide/plugin-development.md` —— 插件开发文档。它被 `AGENTS.md`、`docs/DESIGN.md` § 17
   与 `docs/guide/README.md` 引用，成为后续插件开发的唯一依据。
   （本文档上一版把它写成 `docs/PLUGIN_GUIDE.md`，那个路径**从头到尾没存在过**。）

已确认要处理的差异（来自本次勘察）：

| # | 差异 | 处理 |
| --- | --- | --- |
| 1 | `PluginKind` 代码只有 6 类，`DESIGN.md` § 6.1 与 `02-plugin-api.md` § 2 都写 8 类（多 `image` / `source`） | ✅ 补齐成 8 类。⚠️ **但 `interfaces/image.py` / `interfaces/source.py` 没有跟着落地**——见下方「三处与当初判断不同」 |
| 2 | `02-plugin-api.md` § 7 的 12 个「扩展点」在 `src/` 里**零命中** | ❌ 当初倾向的 `ctx.provide(接口, 实例)` 薄封装**没有做**，改为直接改文档——理由见下 |
| 3 | `02-plugin-api.md` § 3.1 示例写 `api_version = "1"`（字符串），§ 16.1 与代码都要求整数 | ✅ 改示例为裸整数 |
| 4 | `DisturbBudgetConfig` 的 `daily_message_limit_high_urgency` / `high_urgency_threshold` 在 `01-architecture.md` § 2.2 草案里叫别的名字 | ✅ 以代码为准改文档（还发现 `SimulationConfig` 也少一个字段） |
| 5 | `channels/web/__init__.py` 的 89 行 docstring 描述了不会发生的未来（没有 `app.py`） | ✅ 代码落地后重写（批次 C），批次 D 再纠一处 `DEFAULT_SEARCH_PATHS` 的错述 |
| 6 | `interfaces/channel.py` 的 `Direction` / `ChannelCapability` / `MessageKind` 不在 `__all__` | ✅ 补进 `__all__`，并让 `interfaces/__init__.py` 全量转发 44 个名字 |
| 7 | `interfaces/simulation.py` 的 `Stage.run(ctx)` 注解写 `TickContext` 但只在 `TYPE_CHECKING` 下导入 | ✅ 保留（这是正确做法），在文档里说明理由 |
| 8 | `Plugin.on_tick_pre(ctx: Any)` 用 `Any` | ✅ 不是缺陷，是红线 4 的必然结果（`kernel/` 不能 import `sim`）。写进文档，不留哑 `Any` |
| 9 | `cli.py` 的 `metavar` 与真实子命令集 | ✅ 加完新命令组后同步（批次 C） |
| 10 | `PluginStatus` 的 8 个取值里 `DISCOVERED` / `VALIDATED` / `STOPPED` **定义了但无人赋值** | ✅ 在 `discover()` / `load_all()` / `_teardown()` 三处真的赋值，+ 4 个状态机测试 |
| 11 | `DESIGN.md` § 6.1 的八类表与 `02 § 7` 的 12 行表扩展点清单不一致 | ✅ `DESIGN.md` 只留三条别处不重复的结论 + 指针 |
| **12** | 勘察时**没发现**的：`load_manifest` 不拒 `[plugin]` 之外的顶层表 | ✅ 修代码（+ 2 个测试）。**这个 bug 的唯一实例是文档自己的示例** |

**三处与当初判断不同**，记下来因为它们比「按计划做完了」更有信息量：

1. **第 2 项（`ctx.provide()`）最后是改文档，不是加方法。** 当初的理由是「薄封装能让插件作者
   少写一行」。看完 `OwnedRegistry.register` 才发现那一行**并不薄**：它要接 `owner=`
   才能真正做到「卸载时自动回收」（ADR-0007），而一个叫 `provide` 的方法名恰恰让人以为
   不用管归属。**少写一行的代价是写错归属，这笔账不划算。**
   而且 12 个 `provide_*` 里有 3 个按接口各不相同（`provide_tool` 在 `capability` 与 `tool`
   两个 `kind` 下语义不同），统一成一条 `ctx.registry.register(接口, 实例, name=…)` 反而更清楚。
2. **第 1 项的「一并落地 `interfaces/image.py` / `source.py`」没做。** 勘察时把
   「文档说 8 类、代码只有 6 类」当成纯差异，实际上它是一个**已知缺口**（这两类接口
   属于 v0.2.0 / v0.3.0）。所以真正该修的是：`PluginKind` 扩到 8（因为 `Literal` 是
   公共契约，插件会写它），而**接口不落地**，并在文档里明确「清单能过、但今天不生效」。
3. **新增了第 12 项。** 这一项在勘察表里没有——它是在读 `loader.py` 时发现的，
   而且它的「唯一实例」竟然是文档自己的示例（见 `13-interface-consistency.md` § 3.5）。
   教训：**审计表能列出「文档提过的名字在代码里零命中」，列不出「代码根本没检查这件事」。**

---

## 5. 这次明确**不做**的事

写下来是为了下一次不用重新推一遍：

| 不做 | 为什么 |
| --- | --- |
| 真实 IM 渠道（企业微信 / 钉钉 / Telegram）发出站之外的实现 | ADR-0004 已定 v1 只做出站；`wecom` 依赖外置在 optional extra |
| NPC 之间的对话（`npc/` 目前是空壳） | `04-simulation-loop.md` 有规定，但它不阻塞「主体」四块；留给下一轮 |
| `image` / `source` 两大类插件的**具体实现**（生图供应商、RSS 抓取器） | 本批次原计划「把契约与扩展点补齐」（审计第 1、2 项）。**最后两件都没做**：`interfaces/image.py` / `source.py` 未落地，`provide_*()` 也未新增。真正的处理是——`PluginKind` 扩到 8（清单得能过）＋ 在 `02-plugin-api.md` § 2 与指南里明确写「这两个 `kind` 今天只是元数据」。理由见 § 4 批次 D 的「三处与当初判断不同」 |
| 打字延迟的真实 `asyncio.sleep` | `decide_reply` 已经算出延迟值；真睡着会让测试变慢、让 SSE 页面看起来卡住。先把值算出来、展示出来、落库，睡眠交给渠道 |
| 设置页的「立即重启」按钮 | 需要进程管理器，属于运维范畴；页面只显示「有 N 项需重启」 |

---

## 6. 验证方式

```bash
python -m ruff format src tests plugins scripts
python -m ruff check src tests plugins scripts
python -m mypy src/alterego
python -m pytest tests -q --cov=alterego --cov-report=json
python scripts/check_coverage.py
bash scripts/check_architecture.sh
```

抽烟测试（人工，不进 CI）：

```bash
alterego plugins list
alterego config schema --json
alterego chat --once "在忙什么"
alterego serve
```
